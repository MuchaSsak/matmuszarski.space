import "@/index.css";

import { createRoot } from "react-dom/client";

import App from "@/App";

createRoot(document.getElementById("root")!).render(<App />);

// TODO: Loop music
// TODO: Fix z-index in certificatesList
// TODO: Fix projects cards preventing clicking the START button on startup screen
// TODO: Fix crashing on Mars job experience list
